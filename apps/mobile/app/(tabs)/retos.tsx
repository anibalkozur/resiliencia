import ScreenShell from '../../components/ScreenShell';

export default function RetosScreen() {
  return (
    <ScreenShell
      title="RETOS"
      caption="Listado de retos, desafios y marcas del mes."
    />
  );
}